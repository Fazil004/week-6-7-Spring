spring.application.name=Employee Pay roll
# ========== Common Configuration ==========
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Uncomment the following line to activate a specific profile
# spring.profiles.active=dev
# spring.profiles.active=prod

# ========== Development Profile ==========
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:3306/payroll_db
spring.datasource.username=${MYSQL_USER:root}
spring.datasource.password=${MYSQL_PASSWORD:password}

spring.jpa.show-sql=true
logging.level.org.springframework=INFO

# ========== Production Profile ==========
# To use production, comment dev section above and uncomment below

# spring.datasource.url=jdbc:mysql://${MYSQL_HOST}:3306/payroll_db
# spring.datasource.username=${MYSQL_USER}
# spring.datasource.password=${MYSQL_PASSWORD}

# spring.jpa.show-sql=false
# logging.level.org.springframework=INFO
