# Common JPA and Hibernate Config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Default profile active
spring.profiles.active=dev

# Common Logging
logging.level.org.springframework=INFO

# === Dev Profile ===
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:3306/addressbook_db
spring.datasource.username=${MYSQL_USER:root}
spring.datasource.password=${MYSQL_PASSWORD:password}

# === Prod Profile ===
# Uncomment below and comment dev to switch profiles
# spring.datasource.url=jdbc:mysql://${MYSQL_HOST}:3306/addressbook_db
# spring.datasource.username=${MYSQL_USER}
# spring.datasource.password=${MYSQL_PASSWORD}
# spring.jpa.show-sql=false
# logging.level.org.springframework=WARN
